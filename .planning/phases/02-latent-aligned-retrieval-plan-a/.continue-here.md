# HANDOFF — Plan A 训练进行中,新 session 从这里接手

## ⚡⚡⚡⚡⚡ 2026-07-07 凌晨:复现归因全程收官(最新,最优先读;详情 = V1-REPRO-GAP.md 全文 + DIFF-AUDIT.md)

### 归因终局(全部实测,20 repeats,同链)
- **三宇宙三角闭合**:官方 released ckpt 实测 mask-only **0.143±0.005** / full **0.123±0.003** / GT-base 诊断 **0.0235±0.0005**(E0c)→ **published 0.026 唯一匹配 GT-base**(H1 实测落锤);camera-ready MMod 2.835 同归 GT-base 系;arXiv 行逐指标归属:R-P/MM-Dist 吻合 GT-base 带(表内红旗:全面优于 Real Motions 行),FID 0.099 吻合 mask 带 → 疑跨口径混排
- **MoMask 对照**:GT-base 诊断函数 = MoMask 原样继承(momask-codes utils/eval_t2m.py:550 已验证同款);MoMask 论文 0.045 取的是正确口径 → "继承代码口径混淆"解释成立,符合用户"无造假"先验
- **权重法证(DIFF-AUDIT.md)**:我们的树 = 与 released 权重结构一致的唯一可运行实现(官方 ckpt SSTA 键名逐键吻合我们、cond_emb/attnj-attnt 未训练与我们的跳过/注释对齐);pristine master 三项实证无法运行 released 资产;master=一作最终版全量 release(用户确认),ckpt 训练自"训练→开源间被重构掉的内部状态"(18 commits 全历史检索无匹配);权重 lycnight=AIGeeksGroup 同一套(4 ckpt 字节一致);**D4 关闭、E 系列数字全部有效、E12 取消**
- **训练差距不存在且反转**:我们 retrain 双口径优于 released ckpt;V1 retrain vs V2(full,共享官方 rtrans):FID 0.083 vs 0.089(V1 微优),语义指标 V2 全胜;**V2 vs released V1(真 baseline):除 MMod 外全指标显著胜(FID −28%)**
- **主表三行结构提案(待用户定案)**:release 重评 0.123 / +我们改进 retrain 0.083(=rt_in_value minor contribution 行)/ V2 0.089;published 数字不进表只做文字说明

### 在飞与待拍板(2026-07-07 凌晨)
- **RUNNING**:13930 v1_orig(ep40+)、13906/13907 resumes——**建议全停腾 3×L40**(边际价值≈0),等用户点头;E00/E00b/E0c/13915 已收官
- **用户待拍板**:①停 3 job;②主表三行结构;③检索条件化 rtrans 立项与否(唯一存活的 rtrans 项)
- **下一步优先级**:①E09 两格(v1_retrain_nortval / v2_ze_nortval,各 800ep,论文 minor-contribution 消融必需)②I1 调查(E04/E05 四配置字节级相同 → eval_mask 疑不消费 re_dict,数字作废中)③I2 调查(projector R@k 全 chance,κ 表暂缓)④论文按升级版叙事 A 回填 45 占位符 ⑤TMR.pt 在 AIGeeksGroup/ReMoMask HF 仓库(E08-teacher 阻塞已解)
- 集群纪律不变:erinyes 禁用、hades 只 L4、per mem≤15G、eval 必须 ×20、ckpt 用 ep 后缀文件

> 写于 2026-07-05。上一 session 完成了 Phase 2 全部 6 个 plan + 训练流水线 bring-up + V1/V2 对照训练,当前两个训练在 persephone 上稳定运行(~64%)。

## ⚡⚡⚡⚡ 2026-07-06 深夜:收数 sync(最新,最优先读;详情 = RUNS.md 顶部深夜段 + V1-REPRO-GAP §3.8/3.9)

- **E00 中期(10/20)裁决性信号**:官方 ckpt full pipeline 在我们链下 **FID ~0.122、MMod ~1.39**(≠ 论文 0.026/2.84)→ 假设 B(0.026 = GT-base 口径混淆)大增强;同链下我们 retrain 0.083 **反超**官方 ~0.122,"复现差距"重定义为**口径差距**。E00 final + E00b(13918,官方 mask-only ×20)出数后,**与一作对齐 0.026 评估命令 = 最高优先级**。
- **三个新警报**:①E04/E05 四检索配置结果字节级相同 → eval_mask 生成路径疑似不消费检索特征,E04/E05 数字作废,需代码调查(eval_mask→generate 的 re_dict 传递);②projector R@k 全部 chance(基线/κ64/κ1024 均 ~2e-05;连 02-03 冒烟 0.0312=1/32 也是 chance)→ κ 表暂缓,查 R@k 实现 + teacher 自身 R@1;③encoded_texts.npy 被 κ 任务覆写污染 → 13919 已恢复,13907(V2 resume)吃了污染数据(其 ckpt 不会用,损害受限)。
- **13908/13910 其实秒崩没跑**(v1-orig 缺 conf/dataset,已修)→ 重交 **13916(v1_orig)/13917(T1-lowlr)**;rt_in_value-vs-batch 归因现在才开始。
- **13915 逐字复刻 = 0.160**(vs 一作 0.083/我们 0.102)→ 单次 eval FID 方差极大,单次对比全部作废,以 20 次均值为准。
- 在飞:13909(E00,~2h 完)、13916/13917(归因训练)、13918(E00b)、13906/13907(resume,无望刷新 best,13907 污染,**是否 scancel 等用户**)、13919(恢复,分钟级)。磁盘 97%。
- 等用户:①13906/13907 去留;②E00 落锤后如何与一作对话;③E04/E05 调查优先级 vs 论文推进。

## ⚡⚡⚡ 2026-07-06 上午:训练终局 + E01 出数

- **训练结束**:13845/13846 TIMEOUT(3 天墙钟,ep1676/1672),末段无新 best → **ep0409/ep0316 = 最终 best(坐实)**,不重交。
- **E01 正式数字**(20 repeats 完整 pipeline,详表在 RUNS.md 顶部):V1 FID 0.083±0.002 / V2 0.089±0.004;**V2 全语义指标显著占优**(Top1 0.509 vs 0.497、MM-Dist 2.962 vs 3.061);FID 方向与 mask-only 相反 → **叙事等 E02 四格齐再定**(ABLATION-DESIGN 预案 1 激活;rtrans confound → E12 价值上升)。
- **在跑**:13900/13901(E02 mask-only ×20,per L40)、13898(κ1024,per)、13899(z_q 构建,per)、13889(κ64,hades L4)。调度规则:per L40 优先、hades 只用 L4、erinyes 禁用。
- 接手动作:①sacct 查上述五个 job → ②回收 E02 四格 + κ R@k + z_q 库 → ③拿四格数据定叙事方案给用户拍板 → ④E04-eval/E05/E06 按 RUNS「待排第二批」推进。

## ⚡⚡ 2026-07-05 傍晚更新:Phase 4 实验开工(优先读这段)

- **Phase 4 已重定义并开跑**:执行规格 = `.planning/phases/04-comprehensive-evaluation/EXPERIMENTS-SPEC.md`(E01-E12);在飞 job 登记 = 同目录 `RUNS.md`(**新 session 必读**,含手动 sacct 命令)。
- **在飞(13885-13892)**:E01 完整评估 ×2(persephone,RUNNING,~2h/个)、E02 mask-only ×2 + κ∈{64,1024} + z_q 构建(hades,PENDING 等别人放卡)、E03 ✅ 已完成。
- **E03 成果(已入文)**:跨空间秩相关 ρ=0.58±0.16,top-k 近邻重叠 @1/5/10 仅 ~18% → representation gap 实测坐实;数字已进 PLAN-A-FACTS + pami.tex Design Principles 段。
- **G 批代码(G2-G8)已实现+冒烟+scp 至远程**,全向后兼容;实现记录 = phase-04 目录 g-batch-results.json。
- **当日拍板**:E09 = 800ep 截断、**压轴最后跑**;KIT/SnapMoGen = 延后(可能换大服务器);效率图 = 方案 A(整图 L40 重测);rtrans 经代码核实**不吃检索**(transformer_aux 零 re_dict)→ 共享冻结 rtrans 协议干净,完整重训挂 E12(随大服务器批)。
- **⚠️ 新 session 注意**:本 session 的 SLURM Monitor 不跨 session,接手后先跑 RUNS.md 里的 sacct 命令查 job 状态;E01 完成后回收 eval log 数字 → compare_v1_v2.py → 回填 PLACEHOLDERS.md(provisional 口径,停训后转正)。
- **仍开放**:D-P4-01 提前停(870+ epoch 无新 best,建议已给);检索表 ReMoMask-2 行去留;codebook 512/1024 核对;TMR ckpt 下载;HumanML3D.zip 是否传公开 HF repo(AMASS 许可,等拍板)。
- **2026-07-06 增量**:代码/planning/论文/wiki 已 commit 并推送 `AIGeeksGroup/ReMoMask-2`(private)的 **tpami-workspace** 分支(同步命令:`git push origin master:tpami-workspace`);V2 自产资产(全量 database、database_ze、projector、ep0316/ep0409 ckpt+opt.txt)已传 HF `yiranranranra/my_dataset` 的 `remomask2/` 下;**ReMoMask/README.md 重写为 V2 准备指南**(hf-mirror 拉取、stub 库警告、pipeline 命令、eval 陷阱)。

## ⚡ 2026-07-05 深夜更新:论文写作已大规模推进

- **唯一可信 LaTeX = `D:\tpami\currentversion\v2working\`**(解压自 currentversion zip;旧 `_TPAMI_2026__ReMoMask_2/` 作废)。pami.tex 已完成期刊版第一轮全量扩写:摘要/引言(第三轴+3 条新贡献+期刊拓展声明)/related work/design principles(z_e 几何)/方法新增 subsec:lar + subsec:vroute/V2 消融四表+dummy 分析/实现细节/结论/limitations/notation 表,三张结果表已加 ReMoMask-2 高亮占位行。**编译通过(20 页,0 未定义引用)**。
- 写作支撑文档全在 `.planning/paper-v2/`:PLAN-A-FACTS(事实唯一来源)、V2-REVISION-PLAN、FIG1-DESIGN、ABLATION-DESIGN、PLACEHOLDERS(39 处占位符清单)、drafts/(各稿源文件)。
- 自主运行护栏与优先级见 **`D:\tpami\GOAL.md`**(用户已启用 /goal;主线程 Fable 5,机械任务可分发 Sonnet 5 xhigh)。
- 训练夜巡(01:30):V2 ep1284 / V1 ep1280,RUNNING,全局 best 仍为 0.0991@ep409 / 0.1273@ep316,已 870+ epoch 无刷新 → 已达 GOAL G1 提前停建议线,等用户拍板。

## 一句话现状

**V1(Part_TMR)和 V2(z_e)在完全相同配置下单卡对照训练中,V2 全面领先(best FID 0.099 vs 0.127),等训完跑完整 pipeline eval 出论文数字。**

## 训练现状(2026-07-05 快照)

| | V1 baseline | V2 (Plan A) |
|---|---|---|
| 实验名 | `v1_retrain_rtval` | `v2_ze_rtval` |
| 当前 job | **13846** (resume) | **13845** (resume) |
| 进度 | ep ~1268 / 2000 | ep ~1271 / 2000 |
| **全局 best FID** | **0.1273 @ ep316** | **0.0991 @ ep409** |
| 最优 checkpoint | `net_best_fid_ep0316.tar` | `net_best_fid_ep0409.tar` |

- 节点 persephone 各占 1×L40;**erinyes 禁用**(用户规定)
- 配置与 V1 原版 opt.txt 完全一致(latent_dim=512, n_heads=8, n_layers=8, ff_size=1024, batch=64, lr=2e-4 恒定, seed=3407)+ 两边都开 `--rt_in_value`;**唯一变量 = 检索模块**
- ⚠️ lr 恒定不衰减(milestones=1000000),FID 已进平台期(post-resume ~500ep 无一低于 0.099)→ **剩余 epoch 大概率不再刷新 best,可考虑提前停**
- checkpoint 策略:`latest.tar` 每 epoch 覆盖存(resume 无损);`net_best_fid_ep*.tar` 永久保留。⚠️ resume 后 best_fid tracker 重置,无后缀 `net_best_fid.tar` 已被次优覆盖——**最终评估必须显式指定 ep0409/ep0316 文件**
- 历史:原 job 13784/13791 跑到 ep766/762 时节点故障(exit 7),用 `--is_continue` 从 latest.tar 无损续训为 13845/13846
- 看门狗:`~/ReMoMask-2/train_watchdog.sh` 在登录节点后台运行(pgrep 确认 ALIVE),节点故障时自动带 `--is_continue` 重交;**但 watchdog.log 尚未生成,其脚本内容未经实战验证,接手后建议先 cat 检查一遍**
- 监控:`ssh diana.acfr.usyd.edu.au "sacct -j 13845,13846 --format=JobID,JobName%18,State%12,Elapsed -n"`;当前 epoch 看 `grep -ao 'Epoch: [0-9]*' ~/ReMoMask-2/slurm_logs/v2_train_13845.log | tail -1`

## 本 session 做了什么

### 1. Phase 2 Wave 4(02-06)执行完毕 → 6/6 plans complete
- 产物:`scripts/run_plan_a_ablation.sh`、`scripts/compare_v1_v2.py`(输出 comparison.json + comparison.tex)、`scripts/slurm/job0-4*.sh`
- ROADMAP/STATE 已标记 complete;phase 验证 deferred 到训练完成

### 2. 训练流水线 bring-up 中修掉的 bug(均已提交/生效)
| # | 问题 | 修复 |
|---|------|------|
| 1 | BMM teacher 库只有 32 条(config_small 残留) | 全量 train.txt 重建(23,384 条)|
| 2 | `torch.load` 无 map_location(ckpt 存于 cuda:4) | `map_location='cpu'`(build_rag_database.py:96)|
| 3 | DDP `os.makedirs` 竞争 | `exist_ok=True`(base_option.py)|
| 4 | `set_epoch` 被注释(两处) | 取消注释(transformer_trainer_ddp.py:219/476)|
| 5 | projector 缺失时静默 fallback 到 512d → SSTA 崩 | 加 assert(train_mask_transformer_ddp.py:182)|
| 6 | sbatch 脚本漏超参用了默认值(latent_dim 384/n_heads 6)| 显式传 V1 opt.txt 全部参数 |
| 7 | DDP 端口 12584 硬编码,双任务冲突 | 改读 `MASTER_PORT` 环境变量(train_mask_transformer_ddp.py:120)|
| 8 | eval 每 epoch 都跑(C3,~19x 开销) | **用户决定不修**,保留 |

### 3. rt_in_value 定位讨论(用户已确认的结论)
- 来源:Phase 1 ABL-02(R_t 进 SSTA Value 分支,K-only → K+V),zero-cost 结果 FID 0.102→0.089(−13%)
- **原始发表的 V1 是 K-only,没有 rt_in_value**;我们的 V1 retrain 加了它 = "增强版 baseline"(更强,对单变量消融公平)
- **定位:独立 minor contribution**,不与 Plan A 混淆;论文中作为正交消融维度
- ⚠️ −13% 尚未在完整重训 pipeline 复现,坐实前不能当确定筹码

## 待办(按优先级)

1. **[进行中] 等训练到 2000ep** 或确认 FID 饱和后提前停(best 已定型:V2 ep409 / V1 ep316)
2. **[必做] 完整 pipeline 正式评估**:`eval_res.py`(MaskTransformer+ResidualTransformer)× 20 repeats × {V1@ep316, V2@ep409},mean±std → 这才是论文数字,当前所有 FID 都是 MaskTransformer 单独 + 单次的中间量
3. **[必做] rt_in_value 正交消融表**:{Part_TMR, z_e} × {rt_in_value on/off};原始 V1 锚点 = 原文 0.102(或补跑一个不带 rtval 的 V1);同时验证 −13% 是否复现
4. **[然后] `compare_v1_v2.py` 出 comparison.json/tex** + Phase 2 正式验证(`/gsd-verify-work 2` 或 verifier)
5. **[下一阶段] Phase 3 Plan B**(iterative dynamic retrieval,依赖 Phase 2 完成)
6. **[论文] 用户明确:先读透 ECCV 原文再动笔**。LaTeX 工程在 `D:/tpami/_TPAMI_2026__ReMoMask_2/`(含 fig/);上一 session 声称派了读论文的 agent 但结果未返回,**接手后直接自己读**

## 关键路径/命令备忘

- 远程:`ssh diana.acfr.usyd.edu.au`,代码 `~/ReMoMask-2`,conda env `remomask`
- 本地对照图脚本:scratchpad `plot_training2.py`(拼 pre+post resume 日志,输出 `D:/tpami/remote_output/v1_vs_v2_full.png`)
- 日志:`~/ReMoMask-2/slurm_logs/{v2_train_13845,v1_retrain_13846}.log`(pre-resume 段在 13784/13791)
- eval 指标行 grep:`grep '==> Eval' <log>`;best 刷新:`grep 'FID Improved'`
- V2 eval 命令模板(正式评估时):`python eval_res.py ... --use_ze_retrieval --ze_database_path database_ze --projector_path logs/query_projector/best_projector.pt --rt_in_value`(具体见 02-06-SUMMARY.md)

## Critical Anti-Patterns

| 教训 | severity |
|------|----------|
| sbatch 脚本别依赖代码默认超参——必须显式传全 opt.txt 每一项(默认 latent_dim=384 曾导致秒崩) | warning |
| sbatch 里没有 `set -e` 时 Python 崩了 job 仍显示 COMPLETED——所有新 sbatch 脚本必须 `set -e` | warning |
| 远程 sed 注入多行 Python 会踩转义坑——复杂修改先写本地文件再 scp | warning |
